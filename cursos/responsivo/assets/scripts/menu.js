let menu = document.querySelector("#menu")

function showMenu(){
    menu.classList.toggle('showMenu') /* lista de classes que o menu tem (classList); o add adiciona a classe showMenu dentro de menu */
}